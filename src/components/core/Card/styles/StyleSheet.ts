import {StyleSheet} from 'react-native'

import {_w} from '../../../../constants'

export const S = StyleSheet.create({
  image: {
    height: 225,
    marginBottom: 10,
    borderRadius: 20,
    width: _w / 2 - 40
  }
})
